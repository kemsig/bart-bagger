# Install

```
pip install -r requirenments.txt
```

# Usage
```
python3 bart.py --token <your_discord_bot_token> \
                --channel_id <text-channel-ID_where_u_want_ur_notifcations> \
                --url <da jellycat u want> \
                --check-interval <check the robots.txt of the website, default=12s>
```
